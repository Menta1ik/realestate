# Telegram Mini App (Mock) — UAE Real Estate Showcase (v2)

Frontend-only mock Telegram Mini App (WebApp) for a UAE real-estate showcase.
No backend. Uses local mock data (`src/data/mock.ts`).

## Added in v2
- 4th section: **Developers** (filter projects by developer like Emaar)
- Property type filter includes **Townhouse**
- Language switcher **EN/RU** (UI + mock content fields)
- Currency switcher **AED/USD/EUR/GBP/CNY** with automatic conversion (mock mid-market rates)

## Run locally
```bash
npm install
npm run dev
```

Dev server runs on **http://localhost:5174** so your previous version can keep running on 5173.
