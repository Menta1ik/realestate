# QUICK START

1. **Supabase**: Создать проект, скопировать URL/Key.
2. **Backend**: `npm install`, `npx prisma db push`, `npx prisma db seed` (создаст SuperAdmin).
3. **Bot**: В `.env` прописать `SUPER_ADMIN_CHAT_ID` (получить у @userinfobot).
4. **Frontend**: `npm install`, `npm run dev`.

